#!/usr/bin/env bash

cd "$(git rev-parse --show-toplevel)" || exit 1

set -o pipefail

rm -rf alfred-pj.alfredworkflow
zip -r alfred-pj.alfredworkflow images lib *.py LICENSE README.md *.plist icon.*
